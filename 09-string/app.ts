let nombre:string = "matias";
let apellido:string = "luna";
let apellido_nombre = apellido + " " + nombre;
console.log(apellido_nombre);

let cocatenar:string = `${nombre} , ${apellido}`;
console.log(cocatenar.toUpperCase());

